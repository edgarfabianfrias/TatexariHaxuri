class InterstitialSmuggler{

    constructor(){
      this.name = 'TatexaraHaxuri';
      this.gender = 'oracular'
      this.home = 'liminal';
      this.power = "telekenisis";
      this.language = "telepathy";
      this.backgroundSound = "void"; 
      this.seeking = ['transmutation', 'fire', 'water'];
      this.path = 'embodiment';
  
    }
  
    if (planetaryNeed > 0) {
        result = mutate;
    }else {
        result = radicalRespite;
        }



   intervene (){
      //support organisms on earth when threatened
      if (need arises) {
        become (embody organism)
      }
    }

   possesion(){
      //once in host, Tatexari Haxuri bestows power and guidance
      if (embodied) {
       result = mutant power activate;
      } else {
      action = approach;
       result = mutant power engage;
      }
      return result;
      }
    }
    
    process(){
      //Tatexari Haxuri supports neurological integration through conversation and care
       }
  
  }
